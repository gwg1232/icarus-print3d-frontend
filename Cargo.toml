[package]
name = "htmx-tailwind-axum-sqlx-postgres"
version = "0.1.0"
edition = "2024"

[dependencies]
# Backend
axum = { version = "0.8.4", features = ["macros"] }
tokio = { version = "1.47.1", features = ["full"] }

# Frontend
maud = { version = "0.27.0", features = ["axum"] }
